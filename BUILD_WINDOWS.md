# 🚀 Сборка приложения Image Data Annotator для Windows

Это руководство описывает, как собрать полноценное Windows приложение с установщиком.

## 📋 Требования

### Обязательно:
- **Windows 10/11** (64-bit)
- **Python 3.8+** ([скачать](https://www.python.org/downloads/))
- **Git** для клонирования проекта (опционально)

### Для создания установщика:
- **Inno Setup 6** ([скачать](https://jrsoftware.org/isdl.php))

## 🛠️ Подготовка окружения

### 1. Клонируйте или скопируйте проект на Windows машину

```bash
git clone <url-репозитория>
cd ImageDataAnnotator
```

### 2. Создайте виртуальное окружение

```bash
python -m venv venv
```

### 3. Активируйте виртуальное окружение

```bash
venv\Scripts\activate
```

### 4. Установите зависимости

```bash
pip install -r requirements.txt
pip install pyinstaller
```

## 📦 Сборка приложения

### Вариант 1: Автоматическая сборка (рекомендуется)

Просто запустите скрипт сборки:

```bash
build_windows.bat
```

Этот скрипт:
- ✅ Проверит наличие всех зависимостей
- ✅ Очистит предыдущие сборки
- ✅ Соберет приложение с помощью PyInstaller
- ✅ Создаст папку `dist\ImageDataAnnotator\` с готовым приложением

### Вариант 2: Ручная сборка

```bash
# Активируйте виртуальное окружение
venv\Scripts\activate

# Соберите приложение
pyinstaller ImageDataAnnotator_Windows.spec --clean
```

## 🎯 Результат сборки

После успешной сборки вы получите:

```
dist/
└── ImageDataAnnotator/
    ├── ImageDataAnnotator.exe  ← Основной исполняемый файл
    ├── _internal/              ← Библиотеки и зависимости
    └── icon.ico                ← Иконка приложения
```

### Тестирование

Запустите приложение:

```bash
dist\ImageDataAnnotator\ImageDataAnnotator.exe
```

## 📀 Создание установщика

### 1. Установите Inno Setup

Скачайте и установите [Inno Setup 6](https://jrsoftware.org/isdl.php)

### 2. Запустите скрипт создания установщика

```bash
build_installer.bat
```

Этот скрипт:
- ✅ Проверит наличие собранного приложения
- ✅ Найдет Inno Setup Compiler
- ✅ Создаст установщик в папке `dist\installer\`

### 3. Результат

Вы получите установщик:

```
dist/installer/
└── ImageDataAnnotator-Setup-v1.0.0.exe  ← ~50-70 МБ
```

## 📤 Распространение

### Portable версия (без установки)

Просто скопируйте папку `dist\ImageDataAnnotator\` на любой компьютер с Windows:
- ✅ Не требует установки
- ✅ Не требует прав администратора
- ✅ Можно запускать с флешки

### Установщик

Распространяйте файл `ImageDataAnnotator-Setup-v1.0.0.exe`:
- ✅ Автоматическая установка в Program Files
- ✅ Создание ярлыков в меню Пуск и на рабочем столе
- ✅ Простое удаление через Панель управления
- ✅ Профессиональный вид

## 🐛 Решение проблем

### Ошибка "PyInstaller не найден"

```bash
pip install pyinstaller
```

### Ошибка "Python не найден"

Убедитесь, что Python добавлен в PATH:
1. При установке Python отметьте "Add Python to PATH"
2. Или добавьте вручную через Настройки системы

### Приложение не запускается

1. Проверьте, что все зависимости установлены:
   ```bash
   pip install -r requirements.txt
   ```

2. Пересоберите приложение с очисткой:
   ```bash
   build_windows.bat
   ```

3. Проверьте логи в папке `build/ImageDataAnnotator/`

### Inno Setup не найден

Установите Inno Setup 6 по ссылке:
https://jrsoftware.org/isdl.php

## 📝 Структура проекта для сборки

```
ImageDataAnnotator/
├── main.py                             ← Точка входа
├── src/                                ← Исходный код
├── resources/                          ← Ресурсы
│   ├── icon.ico                        ← Иконка для Windows
│   └── icon.png                        ← Иконка PNG
├── ImageDataAnnotator_Windows.spec     ← Конфигурация PyInstaller
├── version_info.txt                    ← Информация о версии
├── installer.iss                       ← Скрипт Inno Setup
├── build_windows.bat                   ← Скрипт сборки
└── build_installer.bat                 ← Скрипт создания установщика
```

## 🎨 Кастомизация

### Изменение иконки

Замените файл `resources/icon.ico` своей иконкой (формат .ico, размеры 16x16 до 256x256)

### Изменение версии

Отредактируйте файлы:
- `version_info.txt` - информация о версии
- `installer.iss` - версия в установщике (строка `#define MyAppVersion`)

### Изменение имени приложения

Отредактируйте:
- `installer.iss` - имя в установщике
- `ImageDataAnnotator_Windows.spec` - имя исполняемого файла

## ✅ Чек-лист перед релизом

- [ ] Протестировано на чистой Windows 10/11
- [ ] Все функции работают корректно
- [ ] Размер установщика приемлемый (~50-100 МБ)
- [ ] Иконки отображаются правильно
- [ ] Приложение запускается без консоли
- [ ] Установщик создает ярлыки корректно
- [ ] Удаление через Панель управления работает

## 📞 Поддержка

При возникновении проблем:
1. Проверьте раздел "Решение проблем" выше
2. Проверьте логи в папке `build/`
3. Убедитесь, что используете актуальную версию Python и зависимостей

---

**Удачной сборки! 🎉**
