# Руководство по интерактивному редактированию позиций элементов

## Обзор

Начиная с версии 2.0, приложение поддерживает **интерактивное редактирование позиций** текстовых элементов и печати прямо в превью. Это позволяет точно настроить расположение элементов, а изменения автоматически применяются ко **всем изображениям** при обработке.

## Активация режима редактирования

### Шаг 1: Откройте превью
1. Перейдите на вкладку **"Предпросмотр"**
2. Нажмите кнопку **"Обновить предпросмотр"** для генерации тестового изображения

### Шаг 2: Включите режим редактирования
1. Нажмите кнопку **"🎯 Режим редактирования"**
2. Превью переключится в интерактивный режим
3. Все текстовые элементы и печать станут перетаскиваемыми

## Работа с элементами

### Перемещение элементов

#### Текстовые поля
- **Наведите** курсор на текст (ИНН, КПП, название поставщика, фиксированные тексты)
- Текст подсветится **синей пунктирной рамкой**
- **Зажмите левую кнопку мыши** и перетащите элемент
- Курсор изменится на ✋ (открытая рука) при наведении
- При захвате курсор изменится на ✊ (закрытая рука)

#### Печать/Подпись
- **Наведите** курсор на печать
- Печать подсветится **зеленой пунктирной рамкой**
- **Зажмите левую кнопку мыши** и перетащите
- Аналогично работает изменение курсора

### Визуальные подсказки

| Состояние | Индикатор |
|-----------|-----------|
| Наведение на текст | Синяя пунктирная рамка, курсор ✋ |
| Наведение на печать | Зеленая пунктирная рамка, курсор ✋ |
| Захват элемента | Курсор ✊ (закрытая рука) |
| Выделенный элемент | Пунктирная рамка |

### Сброс позиций

Если вы хотите вернуть все элементы к исходным позициям:
1. Нажмите кнопку **"Сбросить позиции"**
2. Все элементы вернутся в исходное положение
3. Смещения будут удалены из настроек

## Сохранение изменений

### Автоматическое сохранение
- Смещения **автоматически сохраняются** при каждом перемещении элемента
- Данные сохраняются в файл настроек: `%APPDATA%/ImageDataAnnotator/settings.json`
- Не требуется никаких дополнительных действий

### Применение к обработке
1. После настройки позиций выйдите из режима редактирования
   - Нажмите кнопку **"🔍 Обычный просмотр"**
2. Сохраненные смещения будут **автоматически применены** ко ВСЕМ изображениям при обработке
3. Запустите обработку как обычно через кнопку **"Запустить обработку"**

## Масштабирование в режиме редактирования

Режим редактирования сохраняет все возможности масштабирования:

### Управление масштабом
- **Ctrl + колесо мыши** — увеличение/уменьшение
- **Кнопки +/-** — пошаговое изменение
- **Слайдер** — точная настройка (10%-1000%)
- **"100%"** — оригинальный размер
- **"По размеру окна"** — автоподгонка

### Рекомендации
- Используйте масштаб **200-300%** для точной настройки позиций
- Используйте **"100%"** для проверки реального размера
- Используйте **"По размеру окна"** для общего обзора

## Технические детали

### Формат хранения смещений

Смещения сохраняются в `settings.json` в следующем формате:

```json
{
  "element_offsets": {
    "inn": [15.0, -5.0],
    "kpp": [15.0, -5.0],
    "supplier_label": [0.0, 0.0],
    "supplier_value": [0.0, 0.0],
    "text_1": [10.0, 20.0],
    "text_2": [10.0, 20.0],
    "text_3": [10.0, 20.0],
    "stamp": [50.0, -30.0]
  }
}
```

Где:
- **Ключ** — название элемента
- **Значение** — массив `[dx, dy]` (смещение по X и Y в пикселях)

### Применение смещений

При обработке изображений:
1. Загружаются базовые позиции для каждого элемента
2. К базовой позиции добавляется сохраненное смещение
3. Элемент рисуется в новой позиции: `new_x = base_x + dx`, `new_y = base_y + dy`

### Идентификация элементов

| Элемент | ID в настройках |
|---------|-----------------|
| ИНН | `inn` |
| КПП | `kpp` |
| Заголовок "Поставщик:" | `supplier_label` |
| Название поставщика | `supplier_value` |
| Фиксированный текст 1 | `text_1` |
| Фиксированный текст 2 | `text_2` |
| Фиксированный текст 3 | `text_3` |
| Печать/Подпись | `stamp` |

## Лучшие практики (Context7)

### Используемые технологии PyQt6

1. **QGraphicsItem флаги** (Context7 best practice):
   ```python
   ItemIsMovable       # Позволяет перетаскивать элемент
   ItemIsSelectable    # Позволяет выделять элемент
   ItemSendsGeometryChanges  # Уведомления об изменении позиции
   ```

2. **Event Handlers**:
   - `hoverEnterEvent()` — визуальная подсказка при наведении
   - `hoverLeaveEvent()` — убирает подсветку
   - `mousePressEvent()` — изменение курсора при захвате
   - `mouseReleaseEvent()` — восстановление курсора
   - `itemChange()` — отслеживание изменения позиции

3. **Кастомная отрисовка**:
   - Переопределен метод `paint()` для рисования рамок
   - Использование `QPen` с пунктирным стилем
   - Цветовое кодирование: синий для текста, зеленый для изображений

## Примеры использования

### Сценарий 1: Точная настройка печати
1. Включите режим редактирования
2. Увеличьте масштаб до 200%
3. Перетащите печать в нужное место
4. Проверьте результат на масштабе 100%
5. Выйдите из режима редактирования
6. Запустите обработку — печать будет в новой позиции на всех изображениях

### Сценарий 2: Сдвиг всех текстов вправо
1. Включите режим редактирования
2. Поочередно перетащите каждый текстовый элемент вправо на 20px
3. Система автоматически сохранит изменения
4. Все изображения будут обработаны с новыми позициями

### Сценарий 3: Откат к исходным позициям
1. Если результат не устраивает, включите режим редактирования
2. Нажмите кнопку **"Сбросить позиции"**
3. Все элементы вернутся в исходное положение
4. Смещения будут удалены

## Горячие клавиши

| Действие | Клавиша/Мышь |
|----------|--------------|
| Включить редактирование | Кнопка "🎯 Режим редактирования" |
| Выйти из редактирования | Кнопка "🔍 Обычный просмотр" |
| Перетащить элемент | ЛКМ + перемещение мыши |
| Масштабировать | Ctrl + Колесо мыши |
| Сбросить позиции | Кнопка "Сбросить позиции" |

## Устранение проблем

### Элементы не перетаскиваются
**Решение**: Убедитесь, что включен режим редактирования (кнопка должна быть нажата и показывать "🔍 Обычный просмотр")

### Изменения не применяются при обработке
**Решение**:
1. Проверьте, что вы вышли из режима редактирования (это сохраняет смещения)
2. Проверьте файл настроек `%APPDATA%/ImageDataAnnotator/settings.json` на наличие `element_offsets`

### Элемент улетел за пределы видимости
**Решение**:
1. Нажмите кнопку "Сбросить позиции"
2. Или вручную отредактируйте `settings.json`, удалив раздел `element_offsets`

### Хочу разные позиции для разных изображений
**Ограничение**: Текущая версия применяет одинаковые смещения ко всем изображениям. Для индивидуальной настройки требуется ручное редактирование.

## Архитектура

```
InteractivePreviewWidget
├── InteractivePreviewView (QGraphicsView)
│   ├── QGraphicsScene
│   ├── background_item (QGraphicsPixmapItem)
│   └── draggable_items (Dict)
│       ├── DraggableTextItem (текст)
│       └── DraggablePixmapItem (печать)
└── Панель управления
    ├── Checkbox "Режим редактирования"
    ├── Кнопка "Сбросить позиции"
    └── Элементы масштабирования
```

## Совместимость

- ✅ Работает с позициями "слева" и "снизу"
- ✅ Совместимо со всеми форматами вывода (PNG, JPG, PDF)
- ✅ Сохраняет смещения между сеансами
- ✅ Автоматическое применение к пакетной обработке

## Дальнейшие улучшения

Возможные направления развития:
- [ ] Привязка к сетке (Grid Snapping)
- [ ] Отмена/Повтор действий (Undo/Redo)
- [ ] Индивидуальные позиции для каждого изображения
- [ ] Экспорт/Импорт шаблонов позиций
- [ ] Группировка элементов для одновременного перемещения
